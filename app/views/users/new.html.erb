<% provide(:title, 'Sign up') %>
<h1>微博注册</h1>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for @user do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "注册微博", class: "btn btn-primary" %>	
		<% end %>
	</div>
</div>